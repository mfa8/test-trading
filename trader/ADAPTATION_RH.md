# T2X deployment adaptation — Robinhood agentic MCP
(Read together with RULES.md; RULES.md is verbatim from the author and is never edited.)

## APPENDIX RH — ROBINHOOD ADAPTATION LAYER (added at install, 2026-08-14; authorized by the human's install instruction "it adapts to others at boot")
This deployment runs on the Robinhood agentic MCP, account 445788334 ("Agentic", ••••8334) ONLY. Account ••••3273 is never touched. Never initiate transfers. Tool-role mapping and platform deltas:
- ACCOUNT = get_accounts + get_portfolio (equity=total_value, cash, buying_power). POSITIONS = get_equity_positions. ORDERS_LIST/ORDER_BY_ID = get_equity_orders (order_id; ref_id serves as the idempotency key — record ref_id↔coid pairs in orders.jsonl; there is NO get-by-client-id, so X-4 lookups use get_equity_orders order_id / recent list). PLACE_ORDER = place_equity_order (+review_equity_order as free pre-check). CANCEL_ORDER = cancel_equity_order (no CANCEL_ALL → cancel each id). No REPLACE_ORDER → stop raises are cancel → confirm → re-place (W-5c fallback path). No CLOSE_POSITION → market/limit sell of broker qty. BARS = get_equity_historicals (minute/5minute/day; SIP-quality, no IEX split — X-12's IEX/SIP dance is unnecessary; treat bars as authoritative). SNAPSHOT = get_equity_quotes (real-time two-sided quotes) → data_mode REALTIME_SIP-equivalent; X-3 SIP branch with SOX gate 0.25%. NEWS = none (headline vetoes unavailable; calendar rows stand as written). ACTIVITIES ≈ get_pnl_trade_history / order history. CLOCK/CALENDAR = no tool → use system UTC (ET = UTC−4) + NYSE calendar §10 (no holidays before 9/7); skew assumed 0; verify each session via quote venue timestamps (W-7a self-test uses SPY's 09:30 bar).
- account_mode = IM_FREE. Evidence: get_accounts type="limited_margin", unsettled_funds behavior per RT-6; no PDT fields.
- WHOLE_SHARE = true for all limit orders (Robinhood fractional = market orders, regular hours only). Affordable Tier A at E≈100: TQQQ 77, SQQQ 36, SOXS 40 (+MUU 31 Tier B). SOXL (≈145) unavailable until E ≥ 161 (0.9×E rule).
- stops_mode = NATIVE+SYNTHETIC, with an upgrade: Robinhood stop_market supports GTC on whole shares, so the native stop does NOT expire at 16:00 — no 09:31 re-arm needed (C-5b still runs its gap check and stop-raise; X-7's re-arm clause is satisfied by verification). Stops trigger in regular hours only — overnight protection remains size-based (C-2), as designed.
- EXT_HOURS_OK = true (proven live 2026-08-13: all_day_hours limit orders placed and cancelled). Extended exits use market_hours="extended_hours" or "all_day_hours" limit orders per X-1c.
- WAKE CADENCE DELTA: the platform scheduler cannot fire every minute. Implementation: two cron routines (13:30–19:30 and 14:00–20:00 UTC, weekdays = every 30 min in RTH) are the backstop; during RTH the session self-chains one-shot wakes (send_later, 1-min granularity) to hit every checkpoint: 09:31, 09:36, 09:40→10:30 every 5 min, then :00/:30 checkpoints, 15:00, 15:30, 15:36, 15:50, 15:56, 16:10; while IN_POSITION chain every 2–5 min. Between wakes the GTC native stop is the protection (superior to the manual's day-stop assumption). Off-hours: the manual's fast path maps to simply not scheduling wakes except monitoring (holding: every 15–30 min via send_later; flat: none beyond the crons + Analyst).
- coid format retained in orders.jsonl; ref_id = a fresh UUID per logical order, recorded alongside the coid.
- Repo persistence: trader/ lives in the git repo; every state-changing wake commits and pushes to branch claude/autonomous-swing-trading-hd9m81 (container is ephemeral; the repo is the durable disk).